int compare(int a[]) {
    int i = 0;
    while (i < 6) {
        if (a[i] < a[i + 1])
            putint(1);
        i = i + 1;
    }
    return 0;
}

int main() {
    int a[7] = {1, 2, 2, 3, 4, 1};
    compare(a);
    return 1;
}